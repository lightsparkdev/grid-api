---
title: "Sample Rates"
description: "Explore Grid's competitive FX rates across popular corridors"
icon: "/images/icons/coins.svg"
"og:image": "/images/og/og-get-started.png"
mode: "wide"
---

export const CURRENCIES = [
  { code: 'USD', flag: '🇺🇸', name: 'US Dollar', region: 'Americas', usdMid: 1, gridSpread: 0 },
  { code: 'CAD', flag: '🇨🇦', name: 'Canadian Dollar', region: 'Americas', usdMid: 1.362, gridSpread: 0.35 },
  { code: 'MXN', flag: '🇲🇽', name: 'Mexican Peso', region: 'Americas', usdMid: 17.45, gridSpread: 0.5 },
  { code: 'BRL', flag: '🇧🇷', name: 'Brazilian Real', region: 'Americas', usdMid: 5.05, gridSpread: 0.55 },
  { code: 'CRC', flag: '🇨🇷', name: 'Costa Rican Colón', region: 'Americas', usdMid: 512, gridSpread: 0.8 },
  { code: 'EUR', flag: '🇪🇺', name: 'Euro', region: 'Europe', usdMid: 0.921, gridSpread: 0.25 },
  { code: 'GBP', flag: '🇬🇧', name: 'British Pound', region: 'Europe', usdMid: 0.791, gridSpread: 0.25 },
  { code: 'CHF', flag: '🇨🇭', name: 'Swiss Franc', region: 'Europe', usdMid: 0.881, gridSpread: 0.3 },
  { code: 'DKK', flag: '🇩🇰', name: 'Danish Krone', region: 'Europe', usdMid: 6.87, gridSpread: 0.3 },
  { code: 'SEK', flag: '🇸🇪', name: 'Swedish Krona', region: 'Europe', usdMid: 10.45, gridSpread: 0.4 },
  { code: 'NOK', flag: '🇳🇴', name: 'Norwegian Krone', region: 'Europe', usdMid: 10.55, gridSpread: 0.4 },
  { code: 'CZK', flag: '🇨🇿', name: 'Czech Koruna', region: 'Europe', usdMid: 23.15, gridSpread: 0.45 },
  { code: 'HUF', flag: '🇭🇺', name: 'Hungarian Forint', region: 'Europe', usdMid: 362, gridSpread: 0.5 },
  { code: 'PLN', flag: '🇵🇱', name: 'Polish Zloty', region: 'Europe', usdMid: 4.02, gridSpread: 0.45 },
  { code: 'RON', flag: '🇷🇴', name: 'Romanian Leu', region: 'Europe', usdMid: 4.58, gridSpread: 0.5 },
  { code: 'BGN', flag: '🇧🇬', name: 'Bulgarian Lev', region: 'Europe', usdMid: 1.80, gridSpread: 0.45 },
  { code: 'ISK', flag: '🇮🇸', name: 'Icelandic Króna', region: 'Europe', usdMid: 138, gridSpread: 0.8 },
  { code: 'NGN', flag: '🇳🇬', name: 'Nigerian Naira', region: 'Africa', usdMid: 1550, gridSpread: 0.8 },
  { code: 'KES', flag: '🇰🇪', name: 'Kenyan Shilling', region: 'Africa', usdMid: 129.5, gridSpread: 0.7 },
  { code: 'ZAR', flag: '🇿🇦', name: 'South African Rand', region: 'Africa', usdMid: 18.5, gridSpread: 0.5 },
  { code: 'GHS', flag: '🇬🇭', name: 'Ghanaian Cedi', region: 'Africa', usdMid: 14.8, gridSpread: 0.9 },
  { code: 'UGX', flag: '🇺🇬', name: 'Ugandan Shilling', region: 'Africa', usdMid: 3750, gridSpread: 0.9 },
  { code: 'TZS', flag: '🇹🇿', name: 'Tanzanian Shilling', region: 'Africa', usdMid: 2530, gridSpread: 0.9 },
  { code: 'ZMW', flag: '🇿🇲', name: 'Zambian Kwacha', region: 'Africa', usdMid: 26.5, gridSpread: 1.0 },
  { code: 'MWK', flag: '🇲🇼', name: 'Malawian Kwacha', region: 'Africa', usdMid: 1730, gridSpread: 1.1 },
  { code: 'XOF', flag: '🇸🇳', name: 'West African CFA', region: 'Africa', usdMid: 605, gridSpread: 0.8 },
  { code: 'XAF', flag: '🇨🇲', name: 'Central African CFA', region: 'Africa', usdMid: 605, gridSpread: 0.85 },
  { code: 'CDF', flag: '🇨🇩', name: 'Congolese Franc', region: 'Africa', usdMid: 2780, gridSpread: 1.2 },
  { code: 'BWP', flag: '🇧🇼', name: 'Botswana Pula', region: 'Africa', usdMid: 13.6, gridSpread: 0.9 },
  { code: 'INR', flag: '🇮🇳', name: 'Indian Rupee', region: 'Asia-Pacific', usdMid: 83.5, gridSpread: 0.45 },
  { code: 'PHP', flag: '🇵🇭', name: 'Philippine Peso', region: 'Asia-Pacific', usdMid: 56.2, gridSpread: 0.5 },
  { code: 'IDR', flag: '🇮🇩', name: 'Indonesian Rupiah', region: 'Asia-Pacific', usdMid: 15650, gridSpread: 0.7 },
  { code: 'SGD', flag: '🇸🇬', name: 'Singapore Dollar', region: 'Asia-Pacific', usdMid: 1.34, gridSpread: 0.3 },
  { code: 'HKD', flag: '🇭🇰', name: 'Hong Kong Dollar', region: 'Asia-Pacific', usdMid: 7.81, gridSpread: 0.2 },
  { code: 'CNY', flag: '🇨🇳', name: 'Chinese Yuan', region: 'Asia-Pacific', usdMid: 7.24, gridSpread: 0.6 },
  { code: 'KRW', flag: '🇰🇷', name: 'South Korean Won', region: 'Asia-Pacific', usdMid: 1325, gridSpread: 0.45 },
  { code: 'THB', flag: '🇹🇭', name: 'Thai Baht', region: 'Asia-Pacific', usdMid: 35.2, gridSpread: 0.5 },
  { code: 'VND', flag: '🇻🇳', name: 'Vietnamese Dong', region: 'Asia-Pacific', usdMid: 25200, gridSpread: 0.8 },
  { code: 'MYR', flag: '🇲🇾', name: 'Malaysian Ringgit', region: 'Asia-Pacific', usdMid: 4.65, gridSpread: 0.5 },
  { code: 'LKR', flag: '🇱🇰', name: 'Sri Lankan Rupee', region: 'Asia-Pacific', usdMid: 298, gridSpread: 0.9 },
];

export const SOURCE_CODES = ['USD', 'EUR', 'GBP', 'CAD', 'SGD', 'HKD'];

export const SOURCE_OPTIONS = SOURCE_CODES.map(code => {
  const c = CURRENCIES.find(x => x.code === code);
  return { value: code, flag: c.flag, label: code + ' \u2014 ' + c.name };
});

export const POPULAR_CODES = ['USD', 'EUR', 'GBP', 'CAD', 'MXN', 'BRL', 'INR', 'PHP', 'NGN', 'KES'];

export const formatNumber = (num, decimals) => {
  return num.toLocaleString('en-US', {
    minimumFractionDigits: decimals,
    maximumFractionDigits: decimals,
  });
};

export const getRateDecimals = (rate) => {
  if (rate >= 1000) return 0;
  if (rate >= 100) return 2;
  if (rate >= 1) return 3;
  return 4;
};

export const getReceiveDecimals = (rate) => {
  if (rate >= 100) return 0;
  return 2;
};

export const RateExplorer = () => {
  const [sourceCurrency, setSourceCurrency] = React.useState('USD');
  const [amount, setAmount] = React.useState(10000);
  const [selectedCodes, setSelectedCodes] = React.useState(POPULAR_CODES);
  const [searchQuery, setSearchQuery] = React.useState('');
  const [liveRates, setLiveRates] = React.useState(null);
  const [rateSource, setRateSource] = React.useState(null);

  React.useEffect(() => {
    const fetchGridRates = () =>
      fetch('https://api.lightspark.com/grid/2025-10-13/exchange-rates?sourceCurrency=USD')
        .then(res => {
          if (!res.ok) throw new Error(res.status);
          return res.json();
        })
        .then(json => {
          if (json && json.data && json.data.length > 0) {
            const rates = {};
            json.data.forEach(r => {
              rates[r.destinationCurrency.code] = String(r.exchangeRate);
            });
            setLiveRates(rates);
            setRateSource('grid');
          } else {
            throw new Error('empty');
          }
        });

    const fetchCoinbaseRates = () =>
      fetch('https://api.coinbase.com/v2/exchange-rates?currency=USD')
        .then(res => res.json())
        .then(data => {
          if (data && data.data && data.data.rates) {
            setLiveRates(data.data.rates);
            setRateSource('coinbase');
          }
        });

    fetchGridRates().catch(() => fetchCoinbaseRates().catch(() => {}));
  }, []);

  const getUsdMid = (code) => {
    if (code === 'USD') return 1;
    if (liveRates && liveRates[code]) return parseFloat(liveRates[code]);
    const fallback = CURRENCIES.find(c => c.code === code);
    return fallback ? fallback.usdMid : null;
  };

  const sourceUsdMid = getUsdMid(sourceCurrency);
  const destinations = CURRENCIES.filter(c => c.code !== sourceCurrency);
  const activeCodes = selectedCodes.filter(c => c !== sourceCurrency);

  const allCorridors = destinations.map(dest => {
    const destUsdMid = getUsdMid(dest.code);
    const midRate = destUsdMid / sourceUsdMid;
    const gridRate = midRate * (1 - dest.gridSpread / 100);
    const gridReceive = amount * gridRate;
    const midReceive = amount * midRate;
    const spread = dest.gridSpread;
    return {
      code: dest.code, flag: dest.flag, name: dest.name,
      midRate, gridRate, gridReceive, midReceive, spread,
    };
  });

  const corridors = allCorridors.filter(c => activeCodes.includes(c.code));

  const avgGridPct = corridors.length > 0
    ? corridors.reduce((s, c) => s + (c.gridRate / c.midRate) * 100, 0) / corridors.length : 0;
  const avgSpread = corridors.length > 0
    ? corridors.reduce((s, c) => s + c.spread, 0) / corridors.length : 0;

  const isCustomized = selectedCodes.length !== POPULAR_CODES.length ||
    !POPULAR_CODES.every(c => selectedCodes.includes(c));

  const addableCurrencies = destinations.filter(c => !selectedCodes.includes(c.code));
  const searchMatches = searchQuery.length > 0 ? addableCurrencies.filter(c =>
    c.code.toLowerCase().includes(searchQuery.toLowerCase()) ||
    c.name.toLowerCase().includes(searchQuery.toLowerCase())
  ).slice(0, 8) : [];

  const addCurrency = (code) => {
    setSelectedCodes([...selectedCodes, code]);
    setSearchQuery('');
  };

  const removeCurrency = (code) => {
    setSelectedCodes(selectedCodes.filter(c => c !== code));
  };

  const showAll = () => {
    setSelectedCodes(CURRENCIES.map(c => c.code));
  };

  const resetSelection = () => {
    setSelectedCodes(POPULAR_CODES);
  };

  return (
    <div>
      <div className="rate-explorer not-prose">
        <div className="rate-explorer-controls">
          <div className="rate-explorer-field">
            <label className="rate-explorer-label">You send</label>
            <select
              className="rate-explorer-select"
              value={sourceCurrency}
              onChange={(e) => setSourceCurrency(e.target.value)}
            >
              {SOURCE_OPTIONS.map((opt) => (
                <option key={opt.value} value={opt.value}>
                  {opt.flag} {opt.label}
                </option>
              ))}
            </select>
          </div>
          <div className="rate-explorer-field">
            <label className="rate-explorer-label">Amount</label>
            <div className="rate-explorer-input-wrapper">
              <span className="rate-explorer-input-prefix">{sourceCurrency}</span>
              <input
                type="number"
                className="rate-explorer-input"
                value={amount}
                min={1}
                onChange={(e) => {
                  const val = parseFloat(e.target.value);
                  setAmount(val > 0 ? val : 1);
                }}
              />
            </div>
          </div>
        </div>

        <div className="rate-explorer-summary">
          <span className="rate-explorer-summary-stat">
            <strong>Grid</strong> delivers <strong>{avgGridPct.toFixed(1)}%</strong> of mid-market rate
          </span>
          <span className="rate-explorer-summary-dot">{'\u00b7'}</span>
          <span className="rate-explorer-summary-stat">
            Average spread: <strong>{avgSpread.toFixed(2)}%</strong> across {corridors.length} corridors
          </span>
        </div>

        <div className="rate-explorer-filter">
          {isCustomized ? (
            <>
              <div className="rate-explorer-chips">
                {activeCodes.map(code => {
                  const c = CURRENCIES.find(x => x.code === code);
                  if (!c) return null;
                  return (
                    <span key={code} className="rate-explorer-chip">
                      {c.flag} {code}
                      <button
                        className="rate-explorer-chip-remove"
                        onClick={() => removeCurrency(code)}
                        aria-label={'Remove ' + code}
                      >{'\u00d7'}</button>
                    </span>
                  );
                })}
              </div>
              <div className="rate-explorer-search-wrapper">
                <input
                  type="text"
                  className="rate-explorer-search"
                  placeholder="Add currency..."
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                />
                {searchMatches.length > 0 && (
                  <div className="rate-explorer-dropdown">
                    {searchMatches.map(c => (
                      <div
                        key={c.code}
                        className="rate-explorer-dropdown-item"
                        onMouseDown={(e) => { e.preventDefault(); addCurrency(c.code); }}
                      >
                        <span className="rate-explorer-flag">{c.flag}</span>
                        <span className="rate-explorer-currency-code">{c.code}</span>
                        <span className="rate-explorer-currency-name">{c.name}</span>
                      </div>
                    ))}
                  </div>
                )}
              </div>
              <div className="rate-explorer-filter-actions">
                <button className="rate-explorer-filter-btn" onClick={resetSelection}>Reset</button>
              </div>
            </>
          ) : (
            <>
              <span className="rate-explorer-filter-label">Showing top 10 corridors</span>
              <div className="rate-explorer-search-wrapper">
                <input
                  type="text"
                  className="rate-explorer-search"
                  placeholder="Add currency..."
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                />
                {searchMatches.length > 0 && (
                  <div className="rate-explorer-dropdown">
                    {searchMatches.map(c => (
                      <div
                        key={c.code}
                        className="rate-explorer-dropdown-item"
                        onMouseDown={(e) => { e.preventDefault(); addCurrency(c.code); }}
                      >
                        <span className="rate-explorer-flag">{c.flag}</span>
                        <span className="rate-explorer-currency-code">{c.code}</span>
                        <span className="rate-explorer-currency-name">{c.name}</span>
                      </div>
                    ))}
                  </div>
                )}
              </div>
              <div className="rate-explorer-filter-actions">
                <button className="rate-explorer-filter-btn" onClick={showAll}>Show all</button>
              </div>
            </>
          )}
        </div>

        <div className="rate-explorer-table-wrapper">
          <table className="rate-explorer-table">
            <thead>
              <tr>
                <th>Destination</th>
                <th>Recipient gets (Grid)</th>
                <th>Mid-market</th>
                <th>Spread</th>
              </tr>
            </thead>
            <tbody>
              {corridors.length === 0 && (
                <tr>
                  <td colSpan={4} className="rate-explorer-empty">
                    No currencies selected. Use the search above to add corridors.
                  </td>
                </tr>
              )}
              {corridors.map((row) => {
                const rateDec = getRateDecimals(row.midRate);
                const recDec = getReceiveDecimals(row.midRate);
                return (
                  <tr key={row.code} className="rate-explorer-row">
                    <td>
                      <div className="rate-explorer-dest">
                        <span className="rate-explorer-flag">{row.flag}</span>
                        <span className="rate-explorer-currency-code">{row.code}</span>
                        <span className="rate-explorer-currency-name">{row.name}</span>
                      </div>
                    </td>
                    <td className="rate-explorer-grid-receive">
                      <div className="rate-explorer-receive-amount">
                        {formatNumber(row.gridReceive, recDec)} {row.code}
                      </div>
                      <div className="rate-explorer-rate-subtitle">
                        Rate: {formatNumber(row.gridRate, rateDec)}
                      </div>
                    </td>
                    <td className="rate-explorer-mid-market">
                      <div className="rate-explorer-mid-amount">
                        {formatNumber(row.midReceive, recDec)} {row.code}
                      </div>
                      <div className="rate-explorer-rate-subtitle">
                        Rate: {formatNumber(row.midRate, rateDec)}
                      </div>
                    </td>
                    <td className="rate-explorer-spread">
                      {row.spread.toFixed(2)}%
                    </td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </div>

        <div className="rate-explorer-disclaimer">
          {rateSource === 'grid' ? (
            <>Live mid-market rates via Grid. </>
          ) : rateSource === 'coinbase' ? (
            <>Live mid-market rates via Coinbase. </>
          ) : null}
          Sample rates for illustration only. Actual rates are provided in real time via the <a href="/platform-overview/core-concepts/quote-system">Quote API</a>.
        </div>
      </div>
    </div>
  );
};

Grid provides competitive, real-time FX rates across a wide range of currency corridors. Use the explorer below to see how Grid's rates compare to the mid-market rate across popular corridors.

<RateExplorer />
