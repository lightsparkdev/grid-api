allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../ownership_verifications/OwnershipVerification.yaml
      type:
        type: string
        enum:
          - OWNERSHIP_VERIFICATION.PENDING_REVIEW
          - OWNERSHIP_VERIFICATION.VERIFIED
          - OWNERSHIP_VERIFICATION.FAILED
