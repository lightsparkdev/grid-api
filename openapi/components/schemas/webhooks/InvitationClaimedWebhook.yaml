allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../invitations/UmaInvitation.yaml
      type:
        type: string
        enum:
          - INVITATION.CLAIMED
