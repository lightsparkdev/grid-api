allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../cards/CardTransaction.yaml
      type:
        type: string
        enum:
          - CARD.TRANSACTION_UPDATE
