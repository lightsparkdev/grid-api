allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        allOf:
          - $ref: ../customers/InternalAccount.yaml
          - type: object
            properties:
              oldBalance:
                $ref: ../common/CurrencyAmount.yaml
                description: The account balance before the change
      type:
        type: string
        enum:
          - ACCOUNT.BALANCE_UPDATED
