allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../agents/AgentAction.yaml
      type:
        type: string
        enum:
          - AGENT_ACTION.PENDING_APPROVAL
