allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../customers/CustomerOneOf.yaml
      type:
        type: string
        enum:
          - CUSTOMER.KYC_APPROVED
          - CUSTOMER.KYC_REJECTED
          - CUSTOMER.KYC_PENDING
          - CUSTOMER.KYB_APPROVED
          - CUSTOMER.KYB_REJECTED
          - CUSTOMER.KYB_PENDING
