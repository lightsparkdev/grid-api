allOf:
  - $ref: ./BaseWebhook.yaml
  - type: object
    required:
      - data
    properties:
      data:
        $ref: ../transactions/IncomingPaymentWebhookData.yaml
      type:
        type: string
        enum:
          - INCOMING_PAYMENT.PENDING
          - INCOMING_PAYMENT.COMPLETED
          - INCOMING_PAYMENT.FAILED
