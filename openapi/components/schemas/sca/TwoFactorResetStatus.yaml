type: object
description: >-
  The provider-reported status of an in-progress 2FA reset, polled until it
  reaches the provider's liveness-passed value.
required:
  - status
properties:
  status:
    type: string
    description: The provider-reported status of the reset.
    example: PENDING
