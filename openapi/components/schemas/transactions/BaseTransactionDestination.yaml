type: object
required:
  - destinationType
properties:
  destinationType:
    $ref: ./TransactionDestinationType.yaml
  currency:
    type: string
    description: Currency code for the destination
    example: EUR
