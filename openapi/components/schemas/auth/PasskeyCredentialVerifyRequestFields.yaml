type: object
required:
  - type
  - assertion
properties:
  type:
    type: string
    enum:
      - PASSKEY
    description: Discriminator value identifying this as a passkey verification.
  assertion:
    $ref: ./PasskeyAssertion.yaml
