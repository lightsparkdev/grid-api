type: object
required:
  - type
properties:
  type:
    type: string
    enum:
      - OAUTH
    description: >-
      Discriminator value identifying this as an additional-credential
      challenge for an OAuth credential.
