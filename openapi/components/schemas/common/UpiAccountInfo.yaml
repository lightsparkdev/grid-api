type: object
required:
  - accountType
  - vpa
properties:
  accountType:
    type: string
    enum: [UPI]
    example: UPI
  vpa:
    type: string
    description: Virtual Payment Address for UPI payments
    example: somecustomers@okbank
