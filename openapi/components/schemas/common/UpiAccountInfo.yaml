type: object
required:
  - vpa
  - accountType
properties:
  accountType:
    type: string
    enum:
      - UPI
  vpa:
    type: string
    description: Virtual Payment Address for UPI payments
    example: somecustomers@okbank
