type: object
required:
- accountType
- paymentRails
- sortCode
- accountNumber
properties:
  accountType:
    type: string
    enum:
    - GBP_ACCOUNT
  paymentRails:
    type: array
    items:
      type: string
      enum:
      - FASTER_PAYMENTS
  sortCode:
    type: string
    description: The sort code of the bank
  accountNumber:
    type: string
    description: The account number of the bank
