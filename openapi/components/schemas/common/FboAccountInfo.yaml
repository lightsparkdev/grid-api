type: object
required:
  - accountType
  - currencyCode
properties:
  accountType:
    type: string
    const: FBO
    example: FBO
  currencyCode:
    type: string
    description: Three-letter currency code (ISO 4217)
    example: USD

