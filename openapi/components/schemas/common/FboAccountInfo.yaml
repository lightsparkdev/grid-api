type: object
required:
  - accountType
  - currencyCode
properties:
  accountType:
    const: FBO
    example: FBO
  currencyCode:
    type: string
    description: Three-letter currency code (ISO 4217)
    example: USD

