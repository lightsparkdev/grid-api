title: PKR Account
allOf:
- $ref: ./BasePaymentAccountInfo.yaml
- $ref: ./PkrAccountInfoBase.yaml
- type: object
  required:
  - paymentRails
  - reference
  properties:
    paymentRails:
      type: array
      items:
        type: string
        enum:
        - BANK_TRANSFER
        - MOBILE_MONEY
    reference:
      type: string
      description: Unique reference code that must be included with the payment to
        properly credit it
      example: UMA-Q12345-REF
