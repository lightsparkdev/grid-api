get:
  summary: Look up an UMA address for payment
  description: >
    Lookup a receiving UMA address to determine supported currencies and
    exchange rates.

    This endpoint helps platforms determine what currencies they can send to a
    given UMA address.
  operationId: lookupUma
  tags:
    - Cross-Currency Transfers
  security:
    - BasicAuth: []
  parameters:
    - name: receiverUmaAddress
      in: path
      description: UMA address of the intended recipient
      required: true
      schema:
        type: string
    - name: senderUmaAddress
      in: query
      description: UMA address of the sender (optional if customerId is provided)
      required: false
      schema:
        type: string
    - name: customerId
      in: query
      description: System ID of the sender (optional if senderUmaAddress is provided)
      required: false
      schema:
        type: string
    - name: sendingCurrency
      in: query
      description: >-
        Currency code the sender will send from (e.g., USD). Selects which of the
        sender's configured currencies the response is priced against: it is
        echoed back as the response's `sendingCurrency`, and the
        `minSendingAmount`/`maxSendingAmount` bounds are denominated in its
        smallest unit. Defaults to the sender's default currency.
      required: false
      schema:
        type: string
      example: USD
  responses:
    '200':
      description: Successful lookup
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/receiver/ReceiverUmaLookupResponse.yaml
    '400':
      description: Bad request - Missing or invalid parameters
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error400.yaml
    '401':
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error401.yaml
    '404':
      description: >-
        UMA address not found. The address is well-formed but no receiver
        exists at the counterparty VASP (`UMA_NOT_FOUND`).
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error404.yaml
    '412':
      description: Counterparty doesn't support UMA version
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error412.yaml
    '424':
      description: Counterparty issue
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error424.yaml
    '500':
      description: Internal service error
      content:
        application/json:
          schema:
            $ref: ../../../components/schemas/errors/Error500.yaml
