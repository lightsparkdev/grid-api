'use client';

import type { PhoneProps } from '@/components/Phone';
import { AppShell } from '@/apps/shared/AppShell';

/** Swag phone — empty shell until per-use-case screens land. */
export function PhoneSwag(_props: PhoneProps) {
  return <AppShell />;
}
