/** ChatsApp — the messaging skin's brand + clocks. The brand name is DATA
 *  (read by every face + the registry label); the code id stays `messaging`. */
export const BRAND = 'ChatsApp';

/** Sheet rise/dismiss clock (the shared BottomSheet default). */
export const MESSAGING_SHEET_DURATION = 0.35;
/** Full-screen covers (the card flow's rise) — the pageSheet clock. */
export const MESSAGING_COVER_DURATION = 0.5;

export const MESSAGING_AUTH = {
  welcome: `Welcome to ${BRAND}`,
  legalLead: 'Read our ',
  creating: 'Signing you in\u2026',
};
